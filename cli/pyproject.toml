[project]
name = "stitch-cli"
version = "0.1.0"
description = "CLI tool for Stitch QA"
dependencies = [
    "click",
    "rich",
    "requests",
    "pytest"
]

[project.scripts]
stitch = "stitch_cli.main:cli"